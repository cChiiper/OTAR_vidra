google {
    location = params.gcp_region
    project  = params.gcp_project

    batch {
        spot                = true
        maxSpotAttempts     = 1
        serviceAccountEmail = params.gcp_service_account_email
    }
}
