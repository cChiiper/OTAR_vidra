params {
    input  = "${projectDir}/testdata/example.txt"
    outdir = "${projectDir}/results"
    n_lines = 5
}

process {
    executor = 'local'

    withName: HEADTEXT {
        container = 'headtext:latest'
        cpus = 1
        memory = '1 GB'
        time = '10m'
    }
}
