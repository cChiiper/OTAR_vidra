params {
    input  = "${projectDir}/testdata/example.snappy.parquet"
    outdir = "${projectDir}/results"
    n_lines = 5
}

docker.enabled = true
docker.runOptions = '--platform linux/amd64'

process {
    executor = 'local'

    withName: HEADTEXT {
        container = 'headtext:latest'
    }
}
