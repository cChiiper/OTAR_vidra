process {
    shell = ['/bin/bash', '-euo', 'pipefail']

    withLabel: process_single {
        cpus   = 1
        memory = '1 GB'
        time   = '10m'
    }
}

docker.enabled = false

params {
    input   = null
    outdir  = "results"
    n_lines = 5
}
