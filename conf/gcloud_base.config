executor.name = 'google-batch'

process {
    executor      = 'google-batch'
    stageInMode   = 'copy'
    errorStrategy = 'terminate'
    maxRetries    = 0
    container     = params.bayes_image_registry
}
